for f in $(find ~/.shrc.d/       \( -type f -or -type l \) -regex '.*\.\(sh\|zsh\)' 2>/dev/null|sort); do source "$f"; done
for f in $(find ~/.shrc.d.local/ \( -type f -or -type l \) -regex '.*\.\(sh\|zsh\)' 2>/dev/null|sort); do source "$f"; done
