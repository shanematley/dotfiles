for f in $(find -sE ~/.shrc.d/       \( -type f -or -type l \) -regex '.*\.(sh|zsh)' 2>/dev/null); do source "$f"; done
for f in $(find -sE ~/.shrc.d.local/ \( -type f -or -type l \) -regex '.*\.(sh|zsh)' 2>/dev/null); do source "$f"; done
